# Project Two Environment Configuration
# 二号项目环境配置 / Конфигурация окружения проекта №2

# ============================================================
# Required / 必填项 / Обязательные параметры
# ============================================================

# Crossref API contact email (required for API priority)
# Crossref API 联系邮箱（必须，用于提升API优先级）
CROSSREF_EMAIL=your-email@example.com

# ============================================================
# Optional / 可选项 / Опциональные параметры
# ============================================================

# Chinese name module integration (default: disabled)
# 中文姓名模块集成（默认：关闭）
ENABLE_CHINESE_NAME=false
CHINESE_NAME_MODULE_PATH=

# Decision trace log salt (for privacy protection)
# 决策追踪日志盐值（用于隐私保护）
# ISTINA_LOG_SALT=your-random-salt-here

# Logging level (DEBUG, INFO, WARNING, ERROR)
# 日志级别
# LOG_LEVEL=INFO
